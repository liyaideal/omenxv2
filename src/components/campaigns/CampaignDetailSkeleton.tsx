/**
 * Loading skeleton for /rewards/campaign/:id.
 * Extracted verbatim from LiteCampaignDetailPage (M3a-①) — DOM and classes unchanged.
 */
export const CampaignDetailSkeleton = () => (
  <div className="h-[420px] animate-pulse rounded-[16px] bg-[#0F1114]" />
);
