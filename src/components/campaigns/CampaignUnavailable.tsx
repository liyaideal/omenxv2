/**
 * Empty state for /rewards/campaign/:id when the view no longer resolves.
 * Extracted verbatim from LiteCampaignDetailPage (M3a-①) — DOM and classes unchanged.
 */
export const CampaignUnavailable = () => (
  <div className="py-16 text-center text-[13px] text-[#9AA1AC]">This campaign is no longer available.</div>
);
