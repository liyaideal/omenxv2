# HP-1 · 主页（/）改版第一期 · 实施纪要

日期：2026-08-29 · 依据：CPO 已批 mock（HP-1 工单）

## 改动文件
新增 `src/components/lite/home/`：
- `HomeTape.tsx` — 行情 tape（crypto = intraday round 流；equity = 日收盘股票事件），固定序 BTC→ETH→SOL→NVDA→TSLA→AAPL→MSFT→META，缺数据跳过，全失败不渲染，溢出匀速循环，首载通栏骨架同高。
- `HomeHero.tsx` — hero，桌面/移动分别用 `/assets/desktop|mobile/hero-lynx.png`，<390 隐藏插画。
- `homeShell.tsx` — 卡片/eyebrow/问题行/骨架共享壳。
- `HomeCryptoCard.tsx` — BTC 主卡固定 + ETH/SOL 紧凑卡；模块级 RoundDial 三卡同步；共用 `RoundPlot`；BTC 卡 Round open + LAST 8 点阵。
- `HomeStocksCard.tsx` — Closing today，US·10 / HK·6 页签，四态（可点 / Opens in H:MM / Closed ↑↓ / 骨架）。
- `HomeSportsCard.tsx` — 复用 `buildDayStrip` 与既有 sports 数据。
- `HomeDeskCard.tsx` — 复用 `useEditorPicks` 数据规则，0 条不渲染。
- `HomeStage.tsx` — 桌面 12 栅格 span8/span4（stocks flex 拉伸吃余高），移动单列。

修改：
- `src/pages/lite/LiteEventsPage.tsx` — 接入 tape / hero / 通栏 chips 条 / HomeStage；chips 内容与回调逐字节不变，仅换容器；`stageActive` 常开（tape 需要两条流）；目录改按 subtype 排除 winner 赛果类而非整个 sports；移除旧 `LiteAllStage` / `LiteMobileAllStage` / `LiteEventsGreeting` / All 视图独立 `EditorPicksModule` 挂载。
- `src/components/lite/picks/editorialPicks.ts` — 新增 `optionCount`（多市场判定）。
- `src/index.css` — `lite-tape-scroll` keyframes（respect prefers-reduced-motion）。
- `src/pages/StyleGuide/sections/LiteFinalTouchesSection.tsx` — fixture 补 `optionCount`。

## 红线核对
- Footer 三行 FROZEN：未触碰。
- `LiteEventCard`、`liteListBadges.ts`、目录排序：零改动。
- chips 语义/顺序/行为：零改动。
- 单交易页纪律：所有点击仍只进 `/trade` 或 `/spot`。
- guest 无 Pro 入口：未新增任何入口。
- `src/assets/heroLynx.ts`：不存在，未引用。

## 验收
- tsgo 全量类型检查通过。
- Playwright 1280 / 390 截图：tape、hero、chips 通栏条、双栏舞台、目录、footer 均正常；移动端 ETH/SOL 半宽卡在 390 不溢出。
